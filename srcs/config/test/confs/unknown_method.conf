server {
#hello world
    listen 88;
    server_name localhost;
    location /hello {
        index index.html;
        root /etc/www;
    }
	http_method UNKNOWN;
    location / {
        index index.html;
    }
}
